# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [ src ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install
    - run: npm run-script start
      
    - name: Install SSH Key
      uses: shimataro/ssh-key-action@v2.0.3
      with:
        # SSH private key
        key: ${{ secrets.SFTP_PRIVATE_KEY }}
        # SSH key file name (default: id_rsa)
        name: jbossorg_rsa # optional, default is id_rsa
        # public keys of SSH servers
        known_hosts: filemgmt.jboss.org # default is 
      
    - name: SFTP Deploy
      if: success()
      uses: SamKirkland/FTP-Deploy-Action@3.1.1
      env:
        ARGS: --delete
      with:
        ftp-server: ${{ secrets.SFTP_SERVER }}
        ftp-username: ${{ secrets.SFTP_USER }}
        git-ftp-args: --key ~/.ssh/jbossorg_rsa --remote-root ${{ secrets.SFTP_PATH }}
        local-dir: gh-pages
        
    - name: GitHub Pages Deploy
      if: success()
      uses: crazy-max/ghaction-github-pages@v2
      with:
        target_branch: master
        build_dir: gh-pages
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
